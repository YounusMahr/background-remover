import { CookiePolicy } from '../../components/CookiePolicy';

export const metadata = {
  title: "Cookie Policy - ClearBG Pro",
  description: "Read about how cookies, local sessions, and tracking pixels are configured on our utility platform.",
};

export default function Page() {
  return <CookiePolicy />;
}
